dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn

deployment:
  production:
    branch: master
    commands:
      - yarn now --alias status.evaline.io --token $NOW_TOKEN
