dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - yarn standard

deployment:
  production:
    branch: master
    commands:
      - URL=$(now -- --static -t ${NOW_TOKEN})
      - yarn now -t ${NOW_TOKEN} alias set ${URL} status.evaline.io
