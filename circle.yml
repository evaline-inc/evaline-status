dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn

deployment:
  production:
    branch: master
    commands:
      - yarn now -- --static -t $NOW_TOKEN
      - yarn now alias status.evaline.io
